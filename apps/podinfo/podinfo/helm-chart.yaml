helmChart:
  repo: https://stefanprodan.github.io/podinfo
  chartName: podinfo
  chartVersion: 6.3.0
  releaseName: "{{ args.env_name }}-podinfo"
  namespace: "{{ args.env_name }}-podinfo"
  skipPrePull: true